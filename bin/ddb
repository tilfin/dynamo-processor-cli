#!/usr/bin/env node

'use strict';

const program = require('commander');

program
  .version('0.0.1')
  .command('get table keys', 'DynamoDB getItem')
  .command('put table item', 'DynamoDB putItem')
  .parse(process.argv);
